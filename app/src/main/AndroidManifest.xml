<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.modifica.creditcollectorapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round1"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".Finder" />
        <activity android:name=".Registation" />
        <activity android:name=".Create_loan" />
        <activity android:name=".InfoList" />
        <activity android:name=".getCollection" />
        <activity android:name=".Lone_number_list" />
        <activity android:name=".Collection_list" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Selector" />
        <activity android:name=".TodayTotal_list" />
        <activity android:name=".ToActive_loan" />
        <activity android:name=".TO_Active_loanInfo" />
        <activity android:name=".Offices_list" />
        <activity android:name=".Get_date" />
        <activity android:name=".Due_infor" />
        <activity android:name=".TofinishLoan" />
        <activity android:name=".Finish_info" />
        <activity android:name=".Collaction_info" />
        <activity android:name=".Loan_info" />
        <activity android:name=".Loan_ful_info" />
        <activity android:name=".Loan_info_list" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version"/>

        <activity
            android:name=".Collaction_map"
            android:label="@string/title_activity_collaction_map" />
        <activity android:name=".Date_for_map" />
        <activity
            android:name=".oneMap"
            android:label="@string/title_activity_one_map" />
        <activity android:name=".Online_workers_list" />
        <activity android:name=".comformLoan" />
    </application>

</manifest>